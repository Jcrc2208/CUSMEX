import React from 'react'
import { Routes, Route } from 'react-router-dom'

// Basic routes component placeholder. Add your routes here.
const AppRoutes = () => {
    return (
        <Routes>
            {/* Example: <Route path="/" element={<Home />} /> */}
        </Routes>
    )
}

export default AppRoutes